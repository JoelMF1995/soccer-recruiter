<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="utf-8">
        <title>Liste des club</title>
        <meta name="description" content="club">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    
        <link rel="stylesheet" href="/css/style.css">
    </head>

    <body>  

        <header>
            <div class="topnav">
                <a href="/club" class="active">Liste club</a>
                <a href="/"> Liste des joueurs</a>
                <a href="/joueur/add" > Nouveau Joueur</a>
            </div>
        </header>
    
        <table>
            <tr>
                <th>Nom du Club</th>
                <th>Actions</th>
            </tr>

            <% for (let i = 0 ;  i < clubs.length ; i++) { %>
                <tr>
                    <td>
                        <h3> <%= clubs[i].nomClub%> </h3>
                    </td>
                    <td>
                        <div>
                            <a class="btn btn-warning"  href="/club/update/<%= clubs[i].idclub %>">Modifier</a> 
                            <a class="btn btn-danger"  href="/club/delete/<%= clubs[i].idclub %>">Effacer</a>
                        </div>
                    </td>
                </tr>
                    <% } %>

        </table>

        <div style="width:50%; margin:auto">
            <form action="/club/new" method="POST">
                <div>
                    <label class="form-label" for="nom">Nom du club :</label>
                    <input class="form-control" type="text" id="nom" name="nomClub" value="<%= nomClub %>" required>
                </div>
                <div>
                    <input type="hidden" name="idclub" value="<%= idclub %>">
                    <div class="buttons">
                        <button class="btn btn-success" type="submit">Ajouter</button>
                        <button class="btn btn-danger" onclick="location.href='/club';return false;">Annuler</button>
                    </div>
                </div>
            </form>
        </div>
    </body>  

</html>